/**
 * The Controller package contains classes that handle the control and logic of the application.
 * It acts as an intermediary between the user interface (UI) and the underlying data and business logic.
 *
 * <p>The classes in this package are responsible for processing user input, performing data operations,
 * and coordinating the interactions between the UI and the model components.</p>
 *
 * <p>This package includes the following classes:</p>
 * <ul>
 *   <li>{@link Controller.CustomerController}</li>
 *   <li>{@link Controller.StaffController}</li>
 * </ul>
 *
 * @since 1.0
 * @version 1.0
 */

package Controller;
