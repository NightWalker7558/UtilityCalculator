import View.App;

/**
 * The `Main` class is the entry point for the application. It contains the `main` method to start the application.
 */
public class Main {

  /**
   * The main method that serves as the entry point for the application.
   *
   * @param args Command-line arguments passed to the application (not used in this application).
   */
  public static void main(String[] args) {
    // Create a new instance of the `App` class, initializing the application.
    new App();
  }
}
